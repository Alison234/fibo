package apiserver

import (
	"testing"
)

func TestApiServer_HandleHello(t *testing.T) {
	// s := New(NewConfig())
	// rec := httptest.NewRecorder()
	// req, _ := http.NewRequest(http.MethodGet, "/hello", nil)
	// s.hundleHello().ServeHTTP(rec, req)
	// assert.Equal(t, rec.Body.String(), "HELLO")
}
